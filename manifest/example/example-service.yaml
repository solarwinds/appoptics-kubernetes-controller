apiVersion: "appoptics.io/v1"
kind: "AppOpticsService"
metadata:
  name: exampleservice
  namespace: default
spec:
  namespace: "default"
  secret: "appoptics"
  data: |-
    type: "mail"
    settings:
      addresses: "support@support.io"
    title: "SUPPORT"
